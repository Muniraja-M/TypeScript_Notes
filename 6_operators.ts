var a : number = 20;
var b = 20;
var c = 124;

 c -= b; // Equivalent to c = c - b;

var res = (a - b) + c;

var a = 1;
a++; // a += 1;
a--; // a -= 1; 

// let condition1: boolean = a === b && c == a;
// let condition2: boolean = a != b || c == a;

// Using == for comparison (double equals)
console.log(typeof a);

// ?? 